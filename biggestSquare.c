#include <stdio.h>

int	minPos(int top, int left, int top_left) {
	int output;

	return (output);
}

char **isValidMap(char *map) {
	char **output;

	return (output);
}

char **findBSQ(char **map, char obstacle, char empty) {
	char **output;

	return (output);
}

int main(int argc, char *argv[]) {
	
	return (0);
}
